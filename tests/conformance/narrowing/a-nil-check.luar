--- expect: compile-ok
--- spec: LR57
--- spec: LR8
struct User
    name: string
end

export function greet(user: User?): string
    if user ~= nil then
        return user.name
    end
    return "nobody"
end
