--- expect: compile-error
--- code: LR0160
--- span: 13:19
--- spec: LR57
struct User
    name: string
end

function drain(user: User?, count: int)
    local left = count
    if user ~= nil then
        while left > 0 do
            print(user.name)
            user = nil
            left = left - 1
        end
    end
end
