--- expect: compile-error
--- code: LR0160
--- span: 12:16
--- spec: LR57
struct User
    name: string
end

function greet(user: User?, other: User?): string
    if user ~= nil then
        user = other
        return user.name
    end
    return "nobody"
end
