--- expect: compile-error
--- code: LR0160
--- span: 16:16
--- spec: LR57
struct Profile
    name: string
end

struct User
    profile: Profile?
end

function label(user: User, other: Profile?): string
    if user.profile ~= nil then
        user.profile = other
        return user.profile.name
    end
    return "nobody"
end
