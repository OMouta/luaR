--- expect: compile-ok
--- spec: LR21.1
import config from "./support/config"
import { User, Session as Login } from "./support/models"
import { label } from "./support/deep/nested"

export function main()
    local session: Login = Login { id = "1", user = User { id = "2" } }
    print(config.name(), session.id, label())
end
