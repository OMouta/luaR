--- expect: compile-error
--- code: LR0216
--- span: 10:13
--- spec: LR16.2
--- spec: LR38
export function main()
    local values = [1, 2, 3]
    match values
        case [first, ...rest]
            values:push(first)
            print(rest)
        case _
            print("empty")
    end
end
