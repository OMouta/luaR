--- expect: compile-ok
--- spec: LR20
--- spec: LR76
import { Slug } from "./support/slug"
import { Dashes } from "./support/dashes"

export function main()
    local title = "Hello World"
    print(Slug.slug(title), Dashes.slug(title))
end
