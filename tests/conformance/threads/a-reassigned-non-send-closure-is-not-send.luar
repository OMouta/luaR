--- expect: compile-error
--- code: LR0194
--- span: 20:5
--- spec: LR28
import { Send } from "std/thread"

function transfer<T>(value: T)
where T: Send
    print(value)
end

export function main()
    local values = [1]
    local work: () -> () = function()
        print("ready")
    end
    work = function()
        print(values)
    end
    transfer(work)
end
