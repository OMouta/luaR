--- expect: compile-error
--- code: LR0194
--- span: 17:5
--- spec: LR28
import { Send } from "std/thread"

function transfer<T>(value: T)
where T: Send
    print(value)
end

export function main()
    local count = 0
    local work = function()
        count += 1
    end
    transfer(work)
end
