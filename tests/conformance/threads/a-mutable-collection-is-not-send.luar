--- expect: compile-error
--- code: LR0194
--- span: 14:5
--- spec: LR28
import { Send } from "std/thread"

function transfer<T>(value: T)
where T: Send
    print(value)
end

export function main()
    local values = [1, 2, 3]
    transfer(values)
end
