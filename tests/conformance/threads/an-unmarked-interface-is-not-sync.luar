--- expect: compile-error
--- code: LR0194
--- span: 17:5
--- spec: LR28
import { Sync } from "std/thread"

interface Work
    function run(self)
end

function share<T>(value: T)
where T: Sync
    print(value)
end

function shareWork(work: Work)
    share(work)
end
