export struct Client
    private socket: int = 0
    internal pool: int
    public host: string

    private function handle(self): int
        return self.socket
    end

    function send(self): int
        return self:handle()
    end
end
