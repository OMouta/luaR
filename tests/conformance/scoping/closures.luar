--- expect: compile-ok
--- spec: §9.8
--- spec: §54
export function main()
    local total = 0

    local add = function(amount: int)
        total += amount
    end

    local double = (x: int) => x * 2

    add(double(2))
    print(total)
end
