--- expect: compile-ok
--- spec: LR53
--- spec: LR54
export function main()
    local value = 1

    if value > 0 then
        local value = "shadowed"
        print(value)
    end

    print(value)
end
