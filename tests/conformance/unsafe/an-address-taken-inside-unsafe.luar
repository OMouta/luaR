--- expect: compile-ok
--- spec: LR29.2
--- spec: LR72
@extern("c")
unsafe function clock(out: *mut int): int

export function main()
    local ticks = 0
    unsafe
        local reading = &ticks
        clock(&mut ticks)
        print(reading)
    end
    print(ticks)
end
