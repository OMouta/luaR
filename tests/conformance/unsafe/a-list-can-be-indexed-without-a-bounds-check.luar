--- expect: run
--- exit: 13
--- spec: LR29.2
--- spec: LR70
export function main(): int
    local values: List<int> = [3, 5]
    local frozen = values:frozen()
    unsafe
        local first = values:unchecked(0)
        values:uncheckedSet(1, 10)
        return first + frozen:unchecked(1)
    end
end
