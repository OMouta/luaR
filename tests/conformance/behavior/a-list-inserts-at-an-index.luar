--- expect: run
--- exit: 96
--- spec: LR13.1
export function main(): int
    local values = [1, 2, 3]
    values:insert(0, 10)
    values:insert(4, 40)
    values:insert(2, 20)
    return values[0] + values[2] * 2 + values[5] + values.length
end
