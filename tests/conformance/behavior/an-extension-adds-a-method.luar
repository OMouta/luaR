--- expect: run
--- exit: 8
--- spec: LR20
struct Vec2
    x: int
end

extend Vec2Math for Vec2
    function doubled(self): int
        return self.x * 2
    end
end

export function main(): int
    local point = Vec2 { x = 4 }
    return point:doubled()
end
