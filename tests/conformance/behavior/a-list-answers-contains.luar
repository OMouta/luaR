--- expect: run
--- exit: 13
--- spec: LR13.1
--- spec: LR59
export function main(): int
    local values = [3, 5, 7]
    local names = ["Alice", "Bob"]
    local total = 0
    if values:contains(7) then
        total += 1
    end
    if values:contains(4) then
        total += 100
    end
    if names:contains("Bob") then
        total += 4
    end
    if names:frozen():contains("Carol") then
        total += 100
    end
    local empty = List.new<int>()
    if empty:contains(0) then
        total += 100
    end
    values:push(4)
    if values:contains(4) then
        total += 8
    end
    return total
end
