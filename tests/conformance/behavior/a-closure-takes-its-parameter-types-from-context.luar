--- expect: run
--- exit: 9
--- spec: LR9.2
--- spec: LR9.3
function apply(value: int, f: (int) -> int): int
    return f(value)
end

export function main(): int
    return apply(4, (x) => x * 2) + apply(1, (x) => x)
end
