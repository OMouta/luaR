--- expect: run
--- exit: 225
--- spec: LR13.2
--- spec: LR69
export function main(): int
    local squares: Map<int, int> = Map {}
    for i in 0..<100 do
        squares[i] = i * i
    end
    return (squares[15] ?? 0) + (squares[100] ?? 0)
end
