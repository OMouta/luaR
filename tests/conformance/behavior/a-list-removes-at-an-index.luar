--- expect: run
--- exit: 88
--- spec: LR13.1
--- spec: STD2
export function main(): int
    local values = [5, 6, 7, 8]
    local first = values:removeAt(0)
    local middle = values:removeAt(1)
    local last = values:removeAt(1)
    return first + middle * 2 + last + values[0] * 10 + values.length
end
